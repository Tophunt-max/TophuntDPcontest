{
  "name": "tophunt-archive-import",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "One-time importer that recovers tophunt.in blog posts from the Wayback Machine and loads them into the TopHunt blog (Worker /admin/blog/import -> D1).",
  "scripts": {
    "urls": "node import.mjs --urls-only",
    "dry": "node import.mjs --dry-run --limit=10",
    "import": "node import.mjs"
  },
  "dependencies": {
    "node-html-parser": "^6.1.13"
  }
}
