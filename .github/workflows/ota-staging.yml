name: Expo OTA - Staging Update

on:
  push:
    branches:
      - develop
    paths:
      - 'apps/expo/**'
      - '.github/workflows/ota-staging.yml'

jobs:
  ota_staging:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@vv3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: ./apps/expo
        run: npm install

      - name: Install EAS CLI
        run: npm install -g eas-cli

      - name: Publish to Develop Channel
        working-directory: ./apps/expo
        run: eas update --branch develop --auto
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
